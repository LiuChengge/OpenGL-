# **内窥镜显示系统延迟优化深度分析报告**

## **—— 基于 Vulkan JIT 提交与最新帧策略的效能评估**

## **1\. 核心改进点总结**

通过审查 VkDisplay.cpp 和 endo\_viewer.cpp，本次优化实施了五项关键技术变革：

1. **相位感知的时钟同步 (Phase-Aware Timing)**:  
   在 VkDisplay 中引入 getTimeToNextVSync()，利用 lastPresentTime 和固定的 VSYNC\_PERIOD\_MS (16.667ms) 计算相对于 VSync 的相位偏移，打破了盲目提交的局限。  
2. **准时制提交 (Just-in-Time Submission)**:  
   在主循环中引入 while (timeToVsync \> SUBMIT\_AHEAD\_MS) 等待逻辑。强制 CPU **推迟** 提交渲染命令，直到 VSync 前约 2ms。这极大地压缩了帧在 GPU 命令队列和显示驱动队列中的滞留时间。  
3. **原子级最新帧追踪 (Atomic Freshness Tracking)**:  
   利用 std::atomic\<uint64\_t\> \_frame\_id 实现无锁跨线程通信。渲染线程不再被动处理 FIFO 队列中的每一帧，而是有能力识别并获取采集线程产生的**最新**数据。  
4. **主动丢帧策略 (Active Frame Dropping)**:  
   在 JIT 等待期间（CPU 空闲期），持续轮询 \_frame\_id。如果发现新帧到达（newFrameId \> currentFrameId），立即丢弃手中的旧帧指针，转而处理新帧。这实现了“未渲染即丢弃”，避免了 GPU 算力浪费在陈旧数据上。  
5. **解耦 CPU-GPU 串行依赖**:  
   移除了每帧末尾的 vkDeviceWaitIdle（或 vkQueueWaitIdle），改用 vkWaitForFences。这允许 CPU 在 GPU 处理当前帧显示的同时，提前进入下一帧的逻辑判断（如 JIT 等待和输入轮询），实现了逻辑流水线的并行。

## **2\. 延迟均值下降深度解析**

数据从 **147.97ms** 降至 **86.31ms**，均值减少了 **61.66ms**。这约等于 **3.7 个 60Hz 帧周期**。其物理归因如下：

1. **消除了 "Buffer Pile-up" (缓冲区堆积) \- 贡献约 2-3 帧**:  
   在基准测试中，由于缺乏流控，驱动层的 FIFO 队列通常会积压 2-3 帧。当显示器显示帧 N 时，应用程序可能已经提交了帧 N+2。优化后的 JIT 策略配合 MAX\_FRAMES\_IN\_FLIGHT=1，确保驱动队列深度始终接近于 0，直接削减了 \~33-50ms 的排队延迟。  
2. **"Time Machine" 效应 (最新帧策略) \- 贡献约 1 帧**:  
   在 60Hz 系统中，等待 VSync 的空窗期长达 16ms。  
   * **旧逻辑**: 如果在 VSync 后 1ms 拿到帧 A，必须等 15ms 才能显示。  
   * **新逻辑**: 在等待这 15ms 的过程中，如果相机在第 14ms 生成了帧 B，新策略会丢弃 A，渲染 B。  
   * **结果**: 上屏的数据比旧逻辑“年轻”了整整一个采集周期。  
3. **精确击中 Scanout 窗口 (Scanout Alignment) \- 贡献约 0.5 帧**:  
   SUBMIT\_AHEAD\_MS \= 2.0 确保渲染完成时（假设渲染耗时 \<2ms），恰好位于显示器的 VBlank 期间。这意味着帧被送入显存后，几乎立即被显示控制器（CRTC）读取进行扫描输出，消除了帧在显存中“静止”等待下一次刷新的时间。  
4. **减少上下文切换与同步开销**:  
   移除 vkDeviceWaitIdle 避免了 CPU 每次强制等待 GPU 彻底空闲的 4-5ms 开销（在基准测试中观测到），使 CPU 能更灵敏地响应最新的相机数据中断。

## **3\. 系统稳定性与方差分析**

1. **方差降低的意义 (167.9 \-\> 112.4)**:  
   方差降低表明系统的确定性增强。基准方案中，帧进入队列的时机是随机的，受 OS 调度和驱动状态影响较大。优化方案中，提交时机被软件强制锁定在 VSync 前 2ms，这种“人为制造的规律性”显著降低了抖动。  
2. **上升沿与下降沿的差异**:  
   * **基准**: 上升沿 147ms / 下降沿 158ms。差异较大，说明在负载变化时队列弹性大，不稳定。  
   * **优化**: 上升沿 86ms / 下降沿 99ms。差异缩小，且都在 100ms 以内。这说明即使在最差情况下（下降沿通常代表系统负载增加或相位漂移），JIT 机制也能将延迟控制在可接受范围内。  
3. **建议：动态调整 SUBMIT\_AHEAD\_MS**:  
   当前 2.0ms 是硬编码值。建议增加一个简单的 PID 控制器，根据最近 10 帧的实际渲染耗时（draw\_time），动态调整提前量。如果 GPU 负载增加导致渲染超过 2ms，自动增加提前量以避免错过 VSync。  
4. **建议：防止 CPU 忙等过热**:  
   目前的 while 循环使用 sleep\_for(1ms)。这是合理的，但如果追求极致低功耗，可以结合 nanosleep 或更高精度的定时器。  
5. **监控丢帧率**:  
   日志显示 DROPPED\_FRAMES。这是一个健康指标。如果丢帧率过高（\>50%），说明相机帧率远高于显示帧率，或者 JIT 窗口过窄。当前的逻辑是健康的：丢弃旧帧是为了显示的实时性。
